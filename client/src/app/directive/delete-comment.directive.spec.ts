import { DeleteCommentDirective } from './delete-comment.directive';

describe('DeleteCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteCommentDirective();
    expect(directive).toBeTruthy();
  });
});
