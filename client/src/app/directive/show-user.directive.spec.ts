import { ShowUserDirective } from './show-user.directive';

describe('ShowUserDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowUserDirective();
    expect(directive).toBeTruthy();
  });
});
