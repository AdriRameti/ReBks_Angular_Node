import { UsersProfileDirective } from './users-profile.directive';

describe('UsersProfileDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersProfileDirective();
    expect(directive).toBeTruthy();
  });
});
